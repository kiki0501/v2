version: '3.8'

services:
  vertex-ai-proxy:
    build: .
    container_name: vertex-ai-proxy-v2
    ports:
      - "7860:7860"    # API 端口
      - "28881:28881"  # WebSocket 端口
    volumes:
      # 挂载凭证文件以持久化
      - ./credentials.json:/app/credentials.json
      - ./stats.json:/app/stats.json
      - ./auth_bundle.json:/app/auth_bundle.json
    environment:
      - NOGUI=1
      - PYTHONUNBUFFERED=1
      # ⚠️ 重要：请修改下面的 API_KEY 为你自己的密钥
      # 或者创建 .env 文件并在其中设置 API_KEY=your-key
      - API_KEY=${API_KEY:-your-secret-api-key-here}
      - BROWSER_MODE=${BROWSER_MODE:-headful}  # 可选: manual / websocket / headful
      - DISPLAY=:99  # 用于有头浏览器模式
    restart: unless-stopped
    networks:
      - vertex-proxy-network

networks:
  vertex-proxy-network:
    driver: bridge